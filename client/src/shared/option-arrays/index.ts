import { countryList } from "./country-list";
import { fitnessLevel } from "./fitness-level-list";
import { genderList } from "./gender-list";
import { preferredLanguage } from "./preferred-language-list";

export const optionArrays = {
    countryList,
    fitnessLevel,
    genderList,
    preferredLanguage
}
